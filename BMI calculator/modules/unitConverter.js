const cmToMeters = (height) => height / 100; // gets height in cantimetrs as a argument and convert it into meters
const poundToKg = (weight) => weightInKg = weight * 0.453592; // convert kg into pound
const footInCm = (height) => heightInCm = height * 30.42; // converts cantimeters into foots

module.exports = { cmToMeters, poundToKg, footInCm };